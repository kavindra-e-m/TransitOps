const express = require('express'); const router = express.Router(); router.get('/', (req, res) => res.json([{ id: 1, category: 'Software', description: 'Sub', cost: 200, date: '2026-07-01' }])); router.get('/operational-cost', (req, res) => res.json({ totalOperationalCost: 1500 })); router.post('/', (req, res) => res.status(201).json({ id: 2, ...req.body })); module.exports = router;