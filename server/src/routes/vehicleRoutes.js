const express = require('express'); const router = express.Router(); router.get('/', (req, res) => res.json([{ id: 1, reg_no: 'VAN-05', name: 'Ford Transit', type: 'Van', capacity: 1500, odometer: 12000, acquisition_cost: 35000, status: 'Available' }])); router.get('/check-reg/:regNo', (req, res) => res.json({ isUnique: true })); router.post('/', (req, res) => res.status(201).json({ id: 2, ...req.body, status: 'Available' })); router.put('/:id', (req, res) => res.json({ id: req.params.id, ...req.body })); router.get('/:id/history', (req, res) => res.json({ trips: [], maintenance: [] })); module.exports = router;